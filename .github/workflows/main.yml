name: IPTV All
on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 9 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: 下载指定版本 Chromium
        run: |
          wget https://github.com/macchrome/linchrome/releases/download/v136.7103.97-M136.0.7103.97-r1440670-portable-ungoogled-Lin64/ungoogled-chromium_136.0.7103.97_1.vaapi_linux.tar.xz
          tar xf ungoogled-chromium_136.0.7103.97_1.vaapi_linux.tar.xz

      - name: 查看 Chromium 版本（调试用）
        run: ./ungoogled-chromium_136.0.7103.97_1.vaapi_linux/chrome --version

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: 安装依赖
        run: pip install selenium requests

      - name: 自动安装匹配版本 ChromeDriver
        run: |
          CHROME_VERSION=$(./ungoogled-chromium_136.0.7103.97_1.vaapi_linux/chrome --version | awk '{print $2}' | cut -d '.' -f 1)
          echo "当前Chromium主版本：$CHROME_VERSION"
          DRIVER_VERSION=$(curl -sS "https://chromedriver.storage.googleapis.com/LATEST_RELEASE_${CHROME_VERSION}")
          echo "获取到的ChromeDriver版本：$DRIVER_VERSION"
          wget "https://chromedriver.storage.googleapis.com/${DRIVER_VERSION}/chromedriver_linux64.zip"
          unzip chromedriver_linux64.zip
          sudo mv chromedriver /usr/local/bin/
          rm chromedriver_linux64.zip
          sudo chmod +x /usr/local/bin/chromedriver

      - name: 运行脚本
        run: python ${{ github.workspace }}/IPTV.py

      - name: 提交并推送更改
        run: |
          git config --local user.email "mmgg3381@gmail.com"
          git config --local user.name "aayyy99"
          git add *.txt
          git commit -m "Add generated file" || echo "No changes to commit"
          git push
